README.md
{{ if ne .chezmoi.os "darwin" }}
.zshrc
.Brewfile
.config/nvim
# Omarchy ships these and keeps improving them; let it own them here.
# macOS has no Omarchy, so the shared copies still apply there.
.config/git/config
.config/starship.toml
{{ end }}
{{ if eq .chezmoi.os "darwin" }}
# macOS uses zsh; dot_zshrc.tmpl already sources ~/.config/shell/*.
.bashrc
{{ end }}
